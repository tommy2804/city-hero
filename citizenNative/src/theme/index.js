import { colors, mycolors } from './colors';
import { sizes, space, lineHeights, fonts, fontWeights, fontSizes } from './Text';

export const theme = {
  mycolors,
  colors,
  sizes,
  space,
  lineHeights,
  fonts,
  fontWeights,
  fontSizes,
};
